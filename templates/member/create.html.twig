{% extends 'base.html.twig' %}


{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        {{ form_row(form.schools) }}

        <button type="submit">Create</button>

    {{ form_end(form) }}
{% endblock %}